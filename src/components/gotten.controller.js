/*jshint esnext: true */

(function () {

    'use strict';

    // Controller for "Gotten"
    function GottenController (Data) {
        // The Gotten list is currently view-only. Possible features:
        // - Draggable
        // - Removable
    }
    angular.module('app').controller('GottenController', GottenController);
})();